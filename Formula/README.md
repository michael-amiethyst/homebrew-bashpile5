# Homebrew Formulas

The formula name is at the end of a brew tap command.  E.g. this project is installed
with `brew tap michael-amiethyst/core/bashpile` since the repository name is `homebrew-core`.