# Homebrew Formulas

The formula name is at the end of a brew tap command.  E.g. this project is installed
with `brew tap michael-amiethyst/bashpile5/bashpile` since the repository name is `homebrew-bashpile5`.